# Migration Notes

The project structure was reorganized for maintainability and scalability. Main changes:

- All HTML files moved to public/
- All JS files moved to src/scripts/
- All CSS files moved to src/styles/
- All images moved to assets/images/
- All configuration files moved to config/
- Documentation is in docs/
- Update all HTML <script> and <link> tags to use the new paths
- Update JS imports if any

See README.md for more details.
