# Copilot Instructions

This project uses a professional, scalable structure for a vanilla JS, HTML, and CSS web app. All source code is under src/, static assets and HTML are in public/, images in assets/, configuration in config/, and documentation in docs/. Update all paths in HTML and JS to match this structure. See README.md for migration notes and details.